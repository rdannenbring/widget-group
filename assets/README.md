# Screenshots

Drop image files here. Referenced by the main README and the plugin registry:

- `screenshot.png` — hero shot (a group expanded on the bar). Used by `plugin.json` `screenshot` and shown first in the README.
- `settings.png` — the settings/editor UI.

PNG preferred. Keep them reasonably sized (e.g. ≤ ~1600px wide).
